// This file is for Jest global setup, e.g., for MSW (Mock Service Worker)
// import { server } from './mocks/server'; // Assuming your MSW server setup is in tests/mocks/server.ts

// beforeAll(() => server.listen({ onUnhandledRequest: 'error' }));
// afterAll(() => server.close());
// afterEach(() => server.resetHandlers());

// If you don't have MSW server setup yet, you can leave this file empty or with a placeholder comment.
console.log('Jest setupTests.ts loaded. Configure MSW or other global test setups here.');
