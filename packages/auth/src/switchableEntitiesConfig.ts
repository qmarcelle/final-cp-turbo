// Export config for entities that can trigger plan-switch
export const switchableEntitiesConfig = {
  plans: ['planA', 'planB', 'newMockPlan'], // Example: list of plan IDs that are switchable
  // e.g. { portal: ['member','employer'] }
}
