/**
 * Configuration for entities that can trigger plan switches
 * Defines which plans and portals support dynamic switching
 */
export const switchableEntitiesConfig = {
  plans: ['planA', 'planB', 'newMockPlan'], // List of plan IDs that support switching
  // portals: ['member', 'employer'] // Portal types that support plan switching
}
